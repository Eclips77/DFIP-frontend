# =================================================================
# == Global Environment Variables for Animated Dashboard         ==
# =================================================================
# This file serves as a template. Copy it to .env and fill in your values.

# --- MongoDB Configuration ---
# Your MongoDB connection string.
# For local development, this is typically: mongodb://localhost:27017
# For Docker Compose, you might use a service name: mongodb://mongo:27017
MONGO_URI="mongodb+srv://arieltanami122_db_user:OHod6QgGER7wp09F@facedb.k2ycus.mongodb.net/?retryWrites=true&w=majority&appName=facedb"

# The name of the MongoDB database to use.
MONGO_DB="face_identity"

# --- API Server Configuration (FastAPI) ---
# The port the backend server will run on.
API_PORT=8000

# Comma-separated list of origins allowed to access the API.
# For local development, this should be your frontend's URL.
ALLOWED_ORIGINS="http://localhost:3000,https://dfip-frontend-966e801161c5.herokuapp.com"

# --- Frontend Configuration (Next.js) ---
# The URL of the backend API. This is used by the Next.js app to make requests.
# When running locally (not in Docker), this should point to your local API server.
NEXT_PUBLIC_API_URL="http://localhost:8000"
