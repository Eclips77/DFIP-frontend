# =================================================================
# == Global Environment Variables for DFIP Dashboard             ==
# =================================================================
# This file serves as a template. Copy it to .env and fill in your values.

# --- MongoDB Configuration ---
# Your MongoDB connection string.
# For local development, this is typically: mongodb://localhost:27017
# For production, use your MongoDB Atlas connection string
MONGO_URI="mongodb+srv://arieltanami122_db_user:OHod6QgGER7wp09F@facedb.k2ycus.mongodb.net/?retryWrites=true&w=majority&appName=facedb"

# The name of the MongoDB database to use.
MONGO_DB="face_identity"

# --- API Server Configuration (FastAPI) ---
# The port the backend server will run on.
API_PORT=8000

# Comma-separated list of origins allowed to access the API.
# Include your frontend domains here
ALLOWED_ORIGINS="http://localhost:3000,https://your-frontend-domain.onrender.com"

# --- Collection & Bucket Names ---
# Override collection names if they differ from defaults
ALERTS_COLLECTION_NAME="Event"
GRIDFS_BUCKET_NAME="Photo_storage"

# --- Frontend Configuration (Next.js) ---
# The URL of the backend API. This is used by the Next.js app to make requests.
# For local development
NEXT_PUBLIC_API_URL="http://localhost:8000"

# --- Production Environment Variables ---
# These should be set in your deployment platform (Render)
# NODE_ENV=production
# NEXT_PUBLIC_API_URL=https://your-api-domain.onrender.com
